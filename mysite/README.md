# Projeto Django REST

Para executar o projeto execute o comando dentro deste diretório:

```bash
python manage.py migrate
python manage.py runserver
```

Depois é só acessar a URL: http://127.0.0.1:8000/
